import { UnchecklistDirective } from './unchecklist.directive';

describe('UnchecklistDirective', () => {
  it('should create an instance', () => {
    const directive = new UnchecklistDirective();
    expect(directive).toBeTruthy();
  });
});
