import { AddsubDirective } from './addsub.directive';

describe('AddsubDirective', () => {
  it('should create an instance', () => {
    const directive = new AddsubDirective();
    expect(directive).toBeTruthy();
  });
});
